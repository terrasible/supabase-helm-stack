{{- if .Values.legacySecret.dashboard }}
{{- if not .Values.legacySecret.dashboard.secretRef }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "supabase.secret.dashboard" . }}
  labels:
    {{- include "supabase.labels" . | nindent 4 }}
type: Opaque
data:
{{- range $key, $value := .Values.legacySecret.dashboard }}
{{- if $value }}
{{- if eq (typeOf $value) "string" }}
  {{ $key }}: {{ $value | b64enc }}
{{- end }}
{{- end }}
{{- end }}
{{- end }}
{{- end }}
