{{- if and .Values.auth.enabled .Values.csi.enabled }}
apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: {{ include "supabase.fullname" . }}-auth-csi-secret
  labels:
    {{- include "supabase.labels" . | nindent 4 }}
    app.kubernetes.io/component: auth
  {{- with .Values.csi.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  provider: {{ .Values.csi.provider | default "azure" }}
  parameters:
    usePodIdentity: "{{ .Values.csi.usePodIdentity | default "false" }}"
    useVMManagedIdentity: "{{ .Values.csi.useVMManagedIdentity | default "true" }}"
    userAssignedIdentityID: {{ .Values.csi.userAssignedIdentityID | default "" | quote }}
    keyvaultName: {{ .Values.csi.keyvaultName | required "CSI keyvaultName is required" | quote }}
    cloudName: {{ .Values.csi.cloudName | default "" | quote }}
    objects: |
      array:
        - |
          objectName: supabase-auth-database-url
          objectType: secret
          objectVersion: ""
        - |
          objectName: supabase-auth-jwt-secret
          objectType: secret
          objectVersion: ""
        - |
          objectName: supabase-auth-anon-key
          objectType: secret
          objectVersion: ""
        - |
          objectName: supabase-auth-service-key
          objectType: secret
          objectVersion: ""
        {{- if .Values.csi.auth.additionalSecrets }}
        {{- range .Values.csi.auth.additionalSecrets }}
        - |
          objectName: {{ .objectName }}
          objectType: {{ .objectType | default "secret" }}
          objectVersion: {{ .objectVersion | default "" | quote }}
        {{- end }}
        {{- end }}
  secretObjects:
    - secretName: {{ include "supabase.fullname" . }}-auth-csi-secret
      type: Opaque
      data:
        - objectName: supabase-auth-database-url
          key: database-url
        - objectName: supabase-auth-jwt-secret
          key: jwt-secret
        - objectName: supabase-auth-anon-key
          key: anon-key
        - objectName: supabase-auth-service-key
          key: service-key
        {{- if .Values.csi.auth.additionalSecretObjects }}
        {{- range .Values.csi.auth.additionalSecretObjects }}
        - objectName: {{ .objectName }}
          key: {{ .key }}
        {{- end }}
        {{- end }}
{{- end }}
