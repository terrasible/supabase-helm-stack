{{- if and .Values.realtime.enabled .Values.csi.enabled }}
apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: {{ include "supabase.fullname" . }}-realtime-csi-secret
  labels:
    {{- include "supabase.labels" . | nindent 4 }}
    app.kubernetes.io/component: realtime
  {{- with .Values.csi.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  provider: {{ .Values.csi.provider | default "azure" }}
  parameters:
    usePodIdentity: "{{ .Values.csi.usePodIdentity | default "false" }}"
    useVMManagedIdentity: "{{ .Values.csi.useVMManagedIdentity | default "true" }}"
    userAssignedIdentityID: {{ .Values.csi.userAssignedIdentityID | default "" | quote }}
    keyvaultName: {{ .Values.csi.keyvaultName | required "CSI keyvaultName is required" | quote }}
    cloudName: {{ .Values.csi.cloudName | default "" | quote }}
    objects: |
      array:
        - |
          objectName: supabase-realtime-database-url
          objectType: secret
          objectVersion: ""
        - |
          objectName: supabase-realtime-jwt-secret
          objectType: secret
          objectVersion: ""
        - |
          objectName: supabase-realtime-anon-key
          objectType: secret
          objectVersion: ""
        - |
          objectName: supabase-realtime-service-key
          objectType: secret
          objectVersion: ""
        - |
          objectName: supabase-realtime-secret-key-base
          objectType: secret
          objectVersion: ""
        {{- if .Values.csi.realtime.additionalSecrets }}
        {{- range .Values.csi.realtime.additionalSecrets }}
        - |
          objectName: {{ .objectName }}
          objectType: {{ .objectType | default "secret" }}
          objectVersion: {{ .objectVersion | default "" | quote }}
        {{- end }}
        {{- end }}
  secretObjects:
    - secretName: {{ include "supabase.fullname" . }}-realtime-csi-secret
      type: Opaque
      data:
        - objectName: supabase-realtime-database-url
          key: database-url
        - objectName: supabase-realtime-jwt-secret
          key: jwt-secret
        - objectName: supabase-realtime-anon-key
          key: anon-key
        - objectName: supabase-realtime-service-key
          key: service-key
        - objectName: supabase-realtime-secret-key-base
          key: secret-key-base
        {{- if .Values.csi.realtime.additionalSecretObjects }}
        {{- range .Values.csi.realtime.additionalSecretObjects }}
        - objectName: {{ .objectName }}
          key: {{ .key }}
        {{- end }}
        {{- end }}
{{- end }}
