{{- if and .Values.imgproxy.enabled (and .Values.imgproxy .Values.imgproxy.kong) .Values.imgproxy.kong.enabled }}
apiVersion: configuration.konghq.com/v1
kind: KongService
metadata:
  name: {{ include "supabase.imgproxy.fullname" . }}
  labels:
    {{- include "supabase.labels" . | nindent 4 }}
spec:
  host: {{ include "supabase.imgproxy.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local
  port: {{ .Values.imgproxy.service.port }}
  protocol: http
  {{- with .Values.imgproxy.kong.path }}
  path: {{ .Values }}
  {{- end }}
  {{- with .Values.imgproxy.kong.retries }}
  retries: {{ .Values }}
  {{- end }}
  {{- with .Values.imgproxy.kong.connectTimeout }}
  connect_timeout: {{ .Values }}
  {{- end }}
  {{- with .Values.imgproxy.kong.writeTimeout }}
  write_timeout: {{ .Values }}
  {{- end }}
  {{- with .Values.imgproxy.kong.readTimeout }}
  read_timeout: {{ .Values }}
  {{- end }}
---
{{- if .Values.imgproxy.kong.route.enabled }}
apiVersion: configuration.konghq.com/v1
kind: KongRoute
metadata:
  name: {{ include "supabase.imgproxy.fullname" . }}
  labels:
    {{- include "supabase.labels" . | nindent 4 }}
spec:
  service:
    name: {{ include "supabase.imgproxy.fullname" . }}
  paths:
    {{- toYaml .Values.imgproxy.kong.route.paths | nindent 4 }}
  strip_path: {{ .Values.imgproxy.kong.route.stripPath }}
  {{- with .Values.imgproxy.kong.route.methods }}
  methods:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with .Values.imgproxy.kong.route.protocols }}
  protocols:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
---
{{- if .Values.imgproxy.kong.plugins }}
{{- range $plugin := .Values.imgproxy.kong.plugins }}
apiVersion: configuration.konghq.com/v1
kind: KongPlugin
metadata:
  name: {{ include "supabase.imgproxy.fullname" $ }}-{{ $plugin.name }}
  labels:
    {{- include "supabase.labels" $ | nindent 4 }}
plugin: {{ $plugin.name }}
{{- with $plugin.config }}
config:
  {{- toYaml . | nindent 2 }}
{{- end }}
---
{{- end }}
{{- end }}
{{- end }}
