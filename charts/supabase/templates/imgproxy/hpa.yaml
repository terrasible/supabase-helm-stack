{{- if and .Values.imgproxy.enabled (and .Values.imgproxy .Values.imgproxy.autoscaling) .Values.imgproxy.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "supabase.imgproxy.fullname" . }}
  labels:
    {{- include "supabase.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "supabase.imgproxy.fullname" . }}
  minReplicas: {{ .Values.imgproxy.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.imgproxy.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.imgproxy.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.imgproxy.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.imgproxy.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.imgproxy.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
