{{- if and .Values.rest.enabled (and .Values.rest.monitoring (and .Values.rest .Values.rest.monitoring) .Values.rest.monitoring.enabled) }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "supabase.rest.fullname" . }}
  labels:
    {{- include "supabase.labels" . | nindent 4 }}
    {{- with .Values.rest.monitoring.serviceMonitor.additionalLabels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  selector:
    matchLabels:
      {{- include "supabase.rest.selectorLabels" . | nindent 6 }}
  endpoints:
  - port: http
    interval: {{ .Values.rest.monitoring.serviceMonitor.interval }}
    path: {{ .Values.rest.monitoring.serviceMonitor.path }}
    {{- with .Values.rest.monitoring.serviceMonitor.metricRelabelings }}
    metricRelabelings:
      {{- toYaml . | nindent 4 }}
    {{- end }}
    {{- with .Values.rest.monitoring.serviceMonitor.relabelings }}
    relabelings:
      {{- toYaml . | nindent 4 }}
    {{- end }}
{{- end }}
